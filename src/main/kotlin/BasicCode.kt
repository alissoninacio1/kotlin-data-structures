/**
 * variables can be two types: mutable and read-only(immutable)
 *
 * */

fun variables() {
    var age: Int = 32 //this is mutable
    val customers: Int = 10 //this is read-only(immutable)

    println(age)
    println(customers)
}


